DomainObject <- setRefClass(
  
  'DomainObject',
  
  fields = c(
    id = 'character'
  ),
  
  methods = c(
    
    populateFromDataRow = function(dataRow) {
      
# TODO : fill this in to populate from data row
      
    }
    
  )
  
)
